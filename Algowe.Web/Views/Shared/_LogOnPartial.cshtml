@if(Request.IsAuthenticated) {
    <text>
            Welcome <strong>@User.Identity.Name</strong>! [ @Html.ActionLink("Log Off", "LogOff", "Account") ]
            @if (User.IsInRole(Algowe.Web.Global.AConst.RoleAdmin) || User.IsInRole(Algowe.Web.Global.AConst.RoleSuperAdmin))
            {
                @Html.ActionLink("Admin area", "Index", "Admin");
            }
    </text>
        }
        else {
     <text>&nbsp;</text>
     }
